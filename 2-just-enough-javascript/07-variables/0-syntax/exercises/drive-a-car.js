// #todo

// prettier-ignore
// so prettier does not remove the extra spacing

'use strict';

let phrase = 'drive';

phrase = phrase + ' a';

phrase = phrase + ' car';

console.log(phrase === 'drive a car');

let phrase1 = 'I';

phrase1 = phrase1 + ' eat';

phrase1 = phrase1 + ' an';

phrase1 = phrase1 + ' apple!';

console.log(phrase1);

console.log(phrase1 === 'I eat an apple!');
