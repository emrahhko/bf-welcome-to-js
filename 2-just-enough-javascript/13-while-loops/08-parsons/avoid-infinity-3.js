'use strict';

let dots = '';
// while (dots.length < 5) {
//   dots = dots + '.';
//   alert(dots);
// }

/* --------- */

// dots += '.'; // distractor
while (dots.length < 5) {
  // distractor
  dots = dots + '.';
  console.log(dots);
} // distractor

alert(dots);

// #todo
