'use strict';

let language = 'JavaScript';

let type = typeof language;

console.log(type);

if (type === 'string') {
  console.log('the type is a string');
} else {
  console.log('the type is not a string');
}

// find the type of `language`
//  then compare the type to enter the conditional
// if (__) {
//   console.log(language);
// }
